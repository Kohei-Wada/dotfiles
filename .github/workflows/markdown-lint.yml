name: Markdown-Lint
on:
  push:
    branches: ["main"]
jobs:
  build:

    name: Markdown Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: DavidAnson/markdownlint-cli2-action@v17
