.gitconfig.local

.gnupg/*
!.gnupg/gpg-agent.conf

.aws/*

.env

# Never publish SSH material (keys, config with internal hosts, known_hosts)
.ssh/

# ~/.claude/ — Claude Code working dir. Track only declarative + lockfile bits.
# Most of this dir is auto-managed cache, secrets, or per-project state.
.claude/*
!.claude/settings.json
!.claude/statusline.sh
!.claude/CLAUDE.md
